export default class FileToPublish   {
    constructor(filePath, groupName, forceContentTypeOther) {
        this.filePath = filePath;
        this.groupName = groupName;
        this.forceContentTypeOther = forceContentTypeOther;
    }
}
